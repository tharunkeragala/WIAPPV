{
  "name": "sri-lankan-wedding-invitation",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && npm --prefix client install",
    "dev": "vercel dev",
    "build": "npm --prefix client run build",
    "start": "node api/index.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
